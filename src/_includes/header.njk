<header class="site-header">
    <a class="site-header__title" href="/">ReduxFlakes</a>
    <nav class="site-navbar">
        {% for link in nav.navbar %}
            <a  class="navbar__link" href="{{ link.url }}">{{ link.title }}</a>
        {% endfor %}
    </nav>
</header>