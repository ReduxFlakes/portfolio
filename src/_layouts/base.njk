<!DOCTYPE html>
<html lang="en">
  {% include "head.njk" %}
  <body {% if site_modifier %}
    class="{{site_modifier}}"
  {% endif %}>
    {% block layout %}
    <main {% if content_modifier %}class="{{content_classes}}"{% endif %}>
        {{ content | safe }}
    </main>
    {% endblock %}
    <script
      data-goatcounter="https://reduxflakes.goatcounter.com/count"
      async
      src="//gc.zgo.at/count.js"
    ></script>
  </body>
</html>