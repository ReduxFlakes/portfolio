---
title: "portal"
layout: page.njk
style_slot: true
---

<div class="banner-section">
    <img src="/static/images/branding/banner_dithered.png" alt="">
</div>
<div class="warning-banner">
    <b>ATTENTION:</b> This website is in under heavy reconstruction. If you find any broken
    link, check the <a href="/navstar/">NavStar</a> page.
    </div>
<section class="latest-section">
    <h1>Latest Additions</h1>
    <article-card>
        <img src="/static/images/articles/osm_comp1.png"
            alt="A grid of 4 OS mockups presenting the first OSM Compilation">
        <section class="card-content">
            <h2 class="card-heading">OSM Compilation 1</h2>
            <p class="card-summary">After weeks of work, my first compilation is being released!</p>
        </section>
    </article-card>
    <h2>
        Updates
    </h2>
    <scrollable-box>
        <p>10-10-2024 - Added this...</p>
    </scrollable-box>
</section>
<section class="browse-section">
    <h2>Browse</h2>
    <scrollable-box-flex>
        {% for item in browse %}
            <a href="{{item.link}}" style="text-decoration: none; --active-outline: rgba(255, 255, 255, 0.75);">
                <section class="link-card" style="background: {{item.bg}};">
                    <h3>{{item.title}}
                        <small> - {{item.type}}</small>
                    </h3>
                    <p class="description">{{item.description}}</p>
                </section>
            </a>
        {% endfor %}
    </scrollable-box-flex>
</section>

{% slot "style_slot" %}
<style>
    .banner-section {
        box-shadow: inset 0 0 12px black;
    }

    .banner-section img {
        width: fit-content;
        image-rendering: pixelated;
        z-index: -1;
    }

    .warning-banner {
        text-align: center;
        background: rgb(86, 0, 0);
        color: rgb(255, 84, 84);
    }

    .browse-section,
    .latest-section,
    .warning-banner {
        padding: var(--sp-xs) var(--sp-s);
        border-top: 1px solid rgba(255, 255, 255, 0.3);
    }

    .latest-section {
        display: grid;
        grid-template-columns: 1fr 0.4fr;
        grid-template-rows: auto auto;
        grid-template-areas: "latest-heading updates-heading" "latest-card updates-section";
        gap: var(--sp-s);
        background: var(--darkslateblue);
        color: var(--on-darkslateblue);
    }

    .latest-section > h1 {
        grid-area: latest-heading;
    }

    .latest-section h2 {
        grid-area: updates-heading;
        align-self: self-end;
    }

    .latest-section article-card {
        grid-area: latest-card;
    }

    .latest-section scrollable-box {
        grid-area: updates-section;
    }

    .browse-section {
        background: var(--firebrick);
        color: var(--on-firebrick);
    }

    .link-card {
        width: 31ch;
        height: 100%;
    }

    scrollable-box-flex {
        --scrollbar-theme: #851616 #530a0a;
    }
</style>
{% endslot %}