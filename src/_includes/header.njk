<header class="site-header">
    {% if page.url == "/" %}
        <h1 class="site-header__title">{{site.title}}</h1>
    {% else %}
            <a class="site-header__title" href="/">{{site.title}}</a>
    {% endif %}
</header>