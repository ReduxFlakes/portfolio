{% extends "./single.njk" %}

{% block content %}
    <main class="site--content page--content">
        <h1 id="{{ title | slugify }}" class="page--title">{{ title }}</h1>
        <article id="main--content">
            {{ content | safe }}
        </article>
    </main>
{% endblock %}